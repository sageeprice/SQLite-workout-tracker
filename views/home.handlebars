<body>
  <div class="report">
    <h2>Most recent lifts:</h2>
    {{#each workout}}
      <div>{{this.liftTime}}: {{this.type}} for {{this.reps}} reps at {{this.weight}}</div>
    {{/each}}
  </div>
</body>
